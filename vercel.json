{
    "version": 2,
    "builds": [{
        "src": "/wsgi.py",
        "use": "@vercel/python"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "projectname/wsgi.py"
    }],
    "env": {
        "SECRET_KEY": "secret123",
        "DEBUG": "False"
    }
}